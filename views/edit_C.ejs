<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MODIFICAR COMPRA</title>
    <link rel="stylesheet" href="../resources/css/edit_C.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <a class="homeBtn" href="/tabla_compras"><i class='bx bx-arrow-back'></i></i></a>
    <% if(compras){ %> 

<div class="contenedorRegister">
    <form action= "/update_C" method="POST" class="formRegister">
        <h1>MODIFICAR</h1>
        <input type="hidden" name="id" value="<%= compras.id %>">          
            <div class="inputs">
                usuario_id <input type="number" name="usuario_id" required max="400" min="1" value="<%= compras.usuario_id %>"><br>
            </div>
            <div class="inputs">
                <br> Producto_id <input type="number" value="<%= compras.producto_id %>" name="producto_id" required max="1000" min="1">
            </div>
            <div class="inputs">
                <br>Cantidad <input type="number" value="<%= compras.cantidad %>" name="cantidad" required max="200" min="1">
            </div>
            <div class="inputs">
               $ <input type="number" value="<%= compras.total %>" name="total" required max="9999" min="1">
            </div>
            <div class="inputs">
             Fecha <input type="date" value="<%= compras.fecha %>" name="fecha" required >
            </div>
        
        <button type="submit" class="btn btn-secondary" name="btnguardar">Guardar cambios</button>
    </form>
</div>
<% } %>
</body>
</html>