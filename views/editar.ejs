<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MODIFICAR USUARIO</title>
    <link rel="stylesheet" href="../resources/css/edit_C.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <a class="homeBtn" href="/tabla_crud"><i class='bx bx-arrow-back'></i></i></a>
    <% if(usuarios){ %> 

<div class="contenedorRegister">
    <form action= "/update" method="POST" class="formRegister">
        <h1>MODIFICAR REGISTROS</h1>
        <input type="hidden" name="id" value="<%= usuarios.id %>">          
            <div class="inputs">
                <input type="text" name="nombre_completo" required maxlength="40" minlength="10" value="<%= usuarios.nombre_completo %>">
            </div>
            <div class="inputs">
                <input type="email" value="<%= usuarios.correo %>" name="correo" required maxlength="100" minlength="3">
                <i class='bx bx-envelope'></i>
            </div>
            <div class="inputs">
                <input type="text" value="<%= usuarios.usuario %>" name="usuario" required maxlength="20" minlength="3">
                <i class='bx bx-user'></i>
            </div>
            <div class="select">
                <select name="rol" id="rol" class="select-rol">
                    <option <%= usuarios.rol=='admin' ? "selected" : '' %> value="admin" selected> admin</option>
                    <option <%= usuarios.rol=='visitante' ? "selected" : '' %> value="visitante" selected> visitante </option>
                </select>
            </div>
        
        <button type="submit" class="btn btn-secondary" name="btnguardar">Guardar cambios</button>
    </form>
</div>
<% } %>
</body>
</html>