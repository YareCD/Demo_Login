<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar producto</title>
    <link rel="stylesheet" href="../resources/css/edit_C.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <a class="homeBtn" href="/tabla_productos"><i class='bx bx-arrow-back'></i></i></a>
<div class="contenedorRegister">
    <form action= "/agregar_P" method="POST" class="formRegister">
        <h1>Nuevo producto</h1>         
            <div class="inputs">
                 <input type="text" name="nombre" placeholder="Nombre" required maxlength="40" minlength="5" >
            </div>
            <div class="inputs">
                <input type="number" placeholder="Precio" name="precio" required min="1" max="10000">
            </div>
            <div class="inputs">
                 <input type="number" placeholder="Existencias" name="existencias" required max="100" min="1">
            </div>
            <div class="select">
                <select name="estado" id="estado" class="select-estado">
                    <option value="activo" > activo</option>
                    <option  value="inactivo" > inactivo </option>
                </select>
            </div>
        
        <button type="submit" class="btn btn-secondary" name="btnguardar">Registrar producto</button>
    </form>
</div>

 <!--Referenciado por cdn sweetalert-->
 <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>

 <!--El alert tiene que ser diferente a undefined-->
 
 <% if(typeof alert != "undefined") { %>
      <script>
              Swal.fire
            ({
                  icon: '<%= alertIcon %>',
                  title: '<%= alertTitle %>',
                  text: '<%= alertMessage %>',
                  showConfirmButton: <%= showConfirmButton %>, 
                  timer: <%= timer %>
              }).then(()=>{
                  window.location= '/<%= ruta %>'
              });
      </script>
 <% } %>
</body>
</html>