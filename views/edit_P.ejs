<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MODIFICAR PRODUCTO</title>
    <link rel="stylesheet" href="../resources/css/edit_C.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <a class="homeBtn" href="/tabla_productos"><i class='bx bx-arrow-back'></i></i></a>
    <% if(productos){ %> 

<div class="contenedorRegister">
    <form action= "/update_P" method="POST" class="formRegister">
        <h1>MODIFICAR</h1>
        <input type="hidden" name="id" value="<%= productos.id %>">          
            <div class="inputs">
                Nombre <input type="text" name="nombre" required maxlength="40" minlength="5" value="<%= productos.nombre %>"><br>
            </div>
            <div class="inputs">
                <br> Precio <input type="number" value="<%= productos.precio %>" name="precio" required maxlength="10" minlength="1">
            </div>
            <div class="inputs">
                <br><br>Existencias <input type="number" value="<%= productos.existencias %>" name="existencias" required maxlength="20" minlength="1">
            </div>
           <br><br>Estado
            <div class="select">
                <select name="estado" id="estado" class="select-estado">
                    <option value="activo" <%= productos.estado === 'activo' ? 'selected' : '' %> selected> activo</option>
                    <option  value="inactivo" <%= productos.estado === 'inactivo' ? 'selected' : '' %> selected> inactivo </option>
                </select>
            </div>
        
        <button type="submit" class="btn btn-secondary" name="btnguardar">Guardar cambios</button>
    </form>
</div>
<% } %>
</body>
</html>